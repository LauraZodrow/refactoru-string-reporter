var word = prompt('Enter a word!');
alert(word);
alert(word.length);

var res = word.charAt(2);
alert(res);

var lowerCase = word.toLowerCase();
alert(lowerCase);

var upperCase = word.toUpperCase();
alert(upperCase);

alert("Let's all go to " + word + "!");

alert(word.substring(1,4)); 
